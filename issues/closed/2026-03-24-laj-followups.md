# LAJ 再生成・欠番補完・目視確認

更新日: 2026-03-25

## クローズ

- 2026-03-26 確認完了でクローズ
- `144` も再アノテーション後に確認済み
- 当初の残タスクはすべて解消

## 背景

- `LAJ_017` 基準の再生成を `main/h1/h2/h3/h4/hk/rk/sk` で回した
- `015` と `041`〜`062` のデータ追加後、対象 TSV の再生成を実施した
- `063`〜`150` までの目視確認を完了した
- 当時の残タスクは `144` の再アノテーション依頼のみだった

## 完了

### 1. LAJ_015 の再生成

- `015-mapping*.tsv` を再生成済み
- `015` は確認対象として切り分け済み

### 2. LAJ_041〜062 の作成

- `041`〜`062` の JSON 配置後、`041-mapping*.tsv` 〜 `062-mapping*.tsv` を生成済み
- `047` は JSON の末尾カンマを修正してから再生成

### 3. LAJ_063〜150 の目視チェック

- `063`〜`150` まで確認済み
- 要注意図として `144` を抽出

## 残タスク

- なし
